# Always install devDependencies (TypeScript, @types/*) even when NODE_ENV=production.
# Render sets NODE_ENV=production from stage9-shared during build; without this, tsc is missing.
production=false
